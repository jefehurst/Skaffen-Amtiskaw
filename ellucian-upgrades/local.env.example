# Local environment configuration
# Copy this file to local.env and fill in your values
# local.env is git-ignored and will not be committed

# =============================================================================
# Logseq Integration
# =============================================================================
# Enable HTTP API in Logseq: Settings → Features → Enable HTTP API
# Set your authentication token in: Settings → Features → HTTP API Authentication Token

LOGSEQ_API_TOKEN=your-logseq-api-token-here
LOGSEQ_API_URL=http://localhost:12315

# =============================================================================
# Ellucian Support Portal
# =============================================================================
# Credentials for Ellucian customer support portal
# Used by ellucian-support CLI

ELLUCIAN_SUPPORT_USER=your-ellucian-username
ELLUCIAN_SUPPORT_PW=your-ellucian-password

# =============================================================================
# Runner Technologies Support Portal
# =============================================================================
# Credentials for Runner Technologies support portal
# Used by runner-support CLI

RUNNER_SUPPORT_USER=your-runner-username
RUNNER_SUPPORT_PW=your-runner-password

# =============================================================================
# ESM (Ellucian Solution Manager)
# =============================================================================
# Read-only credentials for ESM access (optional)
# ESM_URL is the real ESM base URL (used for Host header in tunnel mode)
# ESM_TUNNEL_URL is set when accessing ESM through an SSH tunnel:
#   ssh -L 8443:esmhost.internal:443 bastion-host

# ESM_URL=https://esm.example.com/admin
# ESM_TUNNEL_URL=https://localhost:8443
# ESM_USER=readonly
# ESM_PASSWORD=your-esm-password

# =============================================================================
# Atlassian OAuth 2.0 (for MCP server)
# =============================================================================
# Get these from https://developer.atlassian.com/console/myapps/

# ATLASSIAN_OAUTH_CLIENT_ID=your-client-id
# ATLASSIAN_OAUTH_CLIENT_SECRET=your-client-secret
# ATLASSIAN_OAUTH_REDIRECT_URI=http://localhost:8080/callback
# ATLASSIAN_OAUTH_SCOPE="read:jira-work write:jira-work read:confluence-content.all write:confluence-content offline_access"
# ATLASSIAN_OAUTH_CLOUD_ID=your-cloud-id
# CONFLUENCE_URL=https://your-site.atlassian.net/wiki
# JIRA_URL=https://your-site.atlassian.net

# =============================================================================
# OpenAI API (for GPT integration)
# =============================================================================

# OPENAI_API_KEY=sk-your-api-key

# =============================================================================
# HTTP/S Proxy (optional)
# =============================================================================

# HTTP_PROXY=http://proxy.example.com:8080
# HTTPS_PROXY=http://proxy.example.com:8080
# NO_PROXY=localhost,127.0.0.1,.local
